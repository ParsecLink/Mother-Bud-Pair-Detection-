# Pseudo Labels

Current file:

```text
labels_pseudo_mobile_sam_from_source_tifs_mask_aware.csv
```

This is the current source-TIF run with prompted MobileSAM masks. It contains 1528 pseudo-label boxes and all rows are still marked `needs_review`.

Current diagnostics:

```text
mobile_sam_from_source_tifs_mask_aware_diagnostics.csv
```

Older files in this folder are previous experiments and should not be used as the current training source unless intentionally comparing older runs.
